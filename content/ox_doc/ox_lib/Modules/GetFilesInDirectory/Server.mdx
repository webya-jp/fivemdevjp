# サーバー

## lib.getFilesInDirectory

指定されたリソース・ディレクトリにある全ファイルのリストを、ファイル数とともに取得する。

```lua
lib.getFilesInDirectory(path, pattern)
```

* パスがある：`string`
  * ファイルをリストアップするディレクトリへの相対パスまたは絶対パス。
* パターンがある：`string`
  * ファイル検索時にチェックするパターン。

リターンズ

* ファイルである：`string[]`
* ファイル数：`number`

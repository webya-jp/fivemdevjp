# 共有

## lib.getClosestObject

座標セットに最も近いオブジェクトのハンドルと座標を取得します。

```lua
lib.getClosestObject(coords, maxDistance)
```

* コーデ：`vector3`
  * チェックする座標。
* maxDistance?`number`
  * チェックする最大距離。
  * デフォルト：`2.0`

戻る：

* オブジェクト？`number`
* コーデ？`vector3`

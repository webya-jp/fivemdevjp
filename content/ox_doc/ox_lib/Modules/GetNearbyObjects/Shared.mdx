# 共有

## lib.getNearbyObjects

座標セットの範囲内にあるすべてのオブジェクトのハンドルと座標を取得する。

```lua
lib.getNearbyObjects(coords, maxDistance)
```

* コーデ：`vector3`
  * チェックする座標。
* maxDistance?`number`
  * チェックする最大距離。
  * デフォルト：`2.0`

戻る：

* オブジェクトがある：`{ object: number, coords: vector3 }[]`
